node_modules/
dist/
.env
.env.local

# SQLite database and its write-ahead files (created in phase 1)
*.sqlite
*.sqlite-shm
*.sqlite-wal
data/*.db

# Build artifacts from the word-list pipeline are committed; raw sources are not
data/build/sources/

.DS_Store
