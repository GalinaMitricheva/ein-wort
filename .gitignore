node_modules/
dist/
.env
.env.local

# SQLite database and its write-ahead / shared-memory sidecars
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
*.db-journal
data/*.db

# Build artifacts from the word-list pipeline are committed; raw sources are not
data/build/sources/

# Wiktionary verification (scripts/verify-words.ts): cached API responses + report
data/.cache/
data/verify-report.json

.DS_Store
