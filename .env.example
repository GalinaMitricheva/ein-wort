# Copy to .env for local overrides. Optional — the defaults work as-is.
#
# The running app makes NO model API calls and needs no Anthropic credential
# (see docs/architecture.md §7b). Dossiers are pre-built by the offline
# collection task, which runs as Claude Code, not as this app.

# PORT=3000
